# Retrospective 2026-08-23

Covers slices 1, 2, 10–14 and the context-handoff notes; current evidence pack sessions are the three sessions ending at `5da2d3c8`, mapped to S18–S20 by `session_digest.py --limit 20`.

Human-message citations below use the stable `S#:H#` labels from that digest. Note filenames are used where the slice testimony is the evidence source.

## Summary

The same plan/conformance problem has returned after the earlier plan-renderer fix: the human again saw omitted file/function detail and valid existing work as unplanned, while the proposal needed seven revisions and the two active build sessions regenerated the plan 21 times. Performance concerns recurred across both recent build sessions, producing an explicit priority ordering and a request for reusable measurement, but the kit still has no benchmark or distribution-size mechanism in its gate or session contract.

## Quantified evidence

- The current evidence pack contains 3 sessions and 32 human messages.
- It records 83 gate runs, 21 failed invocations, and one conformance failure; schema failed 3 times, format 7 times, typecheck 5 times, and GUT 4 times across the pack.
- The current proposal has 7 revisions. The digest records `python tools/plan_html.py` 14 times in S19 and 7 times in S20, plus 9 schema-description calls in S19.
- S19 also launched Godot 6 times and launched the `--print-fps` variant 4 times. The notes explain that one benchmark measured the menu rather than PLAY and another required manual state/key timing.

## Observations

No human turn is attributed to these. A recurrence nobody ever reacted to is evidence the human does not care, not evidence of a problem, so these are not ranked.

## Finding: performance evidence is a manual experiment, not a reusable benchmark

sessions: [S19; S20]
human_turns: [S19:H2; S19:H3; S20:H5; S20:H6; S20:H11]
mechanical: [Godot launches x10 (S19)]
recurs: true
severity: none
fix_files: [tools/benchmark.py; tools/board.py; tools/plan_html.py]
fix_lines: 200
cost: 30.00
effort: 4
# raw cost 120.00, evidence predates the last change to tools/benchmark.py, tools/board.py, tools/plan_html.py -- discounted 0.25x to 30.00, may already be fixed

_Occurrences: S19:H2, S19:H3, S20:H5, S20:H6, S20:H11; explicit harness decision in `slice-13-renderer-performance.md`; mechanical corroboration: 6 normal Godot launches and 4 `--print-fps` launches in S19._

Low performance was reported during screen transition (S19:H2), followed by a request for an FPS and resource readout (S19:H3), 4 FPS while stationary (S20:H5), a 4 FPS gridline drop and 60 FPS cap (S20:H6), and later movement hitches to 11 FPS (S20:H11). The slice-13 note records the human choosing a reusable benchmark harness instead of continued manual launch and key timing. The note also records a bad first sample taken from the menu, showing that the current procedure can measure the wrong state. The current performance skill says to measure, but supplies no repeatable runner, scenario definition, warm-up rule, or result artifact.

**Proposed kit/process change** — add a reusable tooling harness that launches the existing project in a known mode, waits for import/warm-up, enters PLAY deterministically, runs stationary, movement, and grid scenarios for fixed intervals, and emits JSON/Markdown with engine mode, VSync/FPS-cap settings, average/min/p95 FPS, frame-time outliers, and scenario identity. Let the plan/board link to the latest result and make the harness usable without a model turn. Add a separate export-size probe to the same measurement family rather than treating runtime FPS as a proxy for distribution size.

**Confidence** — high: five observations across two sessions, a documented harness decision, and ten manual launch/measurement commands show a recurring missing tool rather than one bad sample.
**Cost** — medium-high: a host-side runner, deterministic fixtures, result schema, board/plan link, and Windows/Godot process handling.
**Mechanically checkable** — yes. Tests can verify scenario selection, warm-up and sample bounds, stable output schema, and that a run cannot be marked valid when it measured the menu or omitted configuration.

## Finding: baseline and approval history still require manual conformance repair

sessions: [S2; S19; S20]
human_turns: [S2:H7; S2:H10; S19:H6; S20:H4]
mechanical: []
recurs: true
severity: none
fix_files: [tools/plan_html.py; tools/schema.py; tools/conformance.py]
fix_lines: 200
cost: 30.00
effort: 4
# raw cost 120.00, evidence predates the last change to tools/plan_html.py, tools/schema.py, tools/conformance.py -- discounted 0.25x to 30.00, may already be fixed

_Occurrences: S2:H7, S2:H10, S19:H6, S20:H4; corroborating notes: `slice-2-rotation.md`, `slice-12-performance-diagnostics.md`._

The human first reported that existing `red_grid.json` was implemented but appeared as red/unplanned work (S2:H7), then asked why unproposed items and existing functions were absent from the plan (S2:H10). The same complaint returned as “the plan isnt coherent” and “all red” in S19:H6, and as “missing a bunch of details on files and functions and showing unplanned again” in S20:H4. This is not just render churn: proposal revision 1 explicitly says that the baseline comparison omitted uncommitted map and movement files, so valid existing work was reported as unplanned. The render-count finding already deferred in `deferred.json` is treated here only as corroboration; this finding is the unresolved history carry-forward mechanism. The human had already rejected the earlier symptom-level finding, saying its solution was wrong because “there is a historical state that was not carried forward” (`docs/retro/accepted.json`, 2026-08-18).

**Proposed kit/process change** — add a durable slice ledger derived from the last approved baseline, committed history, and the current working-tree delta. Have conformance classify every authored path as carried-forward, new, modified, removed, or omitted, and attach the relevant prior approval/revision to the rendered module, file, and function entry. Make one `proposal doctor` preflight produce schema, baseline, conformance, and hosted-plan diagnostics before rendering; do not rely on the builder to copy an inventory into each new proposal.

**Confidence** — high: four human occurrences across three sessions, a prior rejected fix, seven recorded revisions, and 21 plan renders point to one missing history mechanism.
**Cost** — high: durable state/schema, conformance classification, renderer changes, migration, and regression tests.
**Mechanically checkable** — yes. A test can compare the ledger against `git diff <baseline>`, require a classification for each authored path, and assert that the rendered plan contains no unexplained unplanned item.

## Finding: the hosted plan is not yet a canonical control surface

sessions: [S19; S20]
human_turns: [S19:H5; S19:H19; S19:H20; S20:H4; S20:H10]
mechanical: []
recurs: true
severity: none
fix_files: [tools/board.py; tools/board_client.py; tools/retro_html.py; tools/plan_html.py]
fix_lines: 150
cost: 25.00
effort: 4
# raw cost 100.00, evidence predates the last change to tools/board.py, tools/board_client.py, tools/retro_html.py, tools/plan_html.py -- discounted 0.25x to 25.00, may already be fixed

_Occurrences: S19:H5, S19:H19, S19:H20, S20:H4, S20:H10._

The human asked for a server-side Play control to launch the local game and avoid spending a chat turn on “run the game” (S19:H5), then asked whether the launch item had actually been captured (S19:H20). They reported the static-plan link twice (S19:H19 and S20:H4), and still had to say “ok run the game” in S20:H10. The generated plan currently has relative navigation links while its retrospective banner uses an absolute `/retro.html` link; the board contract has no game-launch or benchmark endpoint. The documentation describes a hosted board and a file:// fallback, but does not provide one canonical hosted URL or a game-control state, so the advertised control-hub workflow is incomplete.

**Proposed kit/process change** — centralise URL generation in the shared page client: when served, emit the board origin and canonical plan/retro routes; when opened via file://, label controls read-only rather than presenting a misleading static link. Add an explicit, safety-scoped board action for launching the project and expose its last-known state, plus a link to the resumable builder/benchmark run. Do not add game code; this is local tooling and page workflow.

**Confidence** — high: the request and follow-up recur across sessions, and the current generated-page/code surfaces contain navigation but no corresponding launch mechanism.
**Cost** — medium-high: URL contract, local-process lifecycle, permissions/error handling, UI state, and browser/integration tests.
**Mechanically checkable** — yes. Served-page tests can assert canonical same-origin links, file:// read-only behavior, and launch status transitions; an integration fixture can verify a bounded local process invocation without starting the real game.

## Priority order for promotion

1. Baseline/history-aware conformance and durable approval ledger — it is the highest direct source of repeated planning waste and can invalidate every later review.
2. Priority contract plus reusable benchmark/export-size evidence — the human explicitly made this high priority, and it supplies the measurements needed to enforce it without guessing.
3. Canonical hosted control surface and launch state — it removes repeated manual turns and makes the new evidence tools usable.

No source, gate, rule, skill, agent definition, proposal, or file outside `docs/retro/` was changed by this retrospective.

## Finding: the newly stated priority ordering is not an agent or gate contract

sessions: [S20]
human_turns: [S20:H6]
mechanical: []
recurs: false
severity: none
fix_files: [AGENTS.md; .github/agents/game-builder.agent.md; tools/schema.py; tools/plan_html.py]
fix_lines: 120
cost: 2.50
effort: 4
# raw cost 10.00, evidence predates the last change to AGENTS.md, .github/agents/game-builder.agent.md, tools/schema.py, tools/plan_html.py -- discounted 0.25x to 2.50, may already be fixed

_Occurrences: one independent human statement, S20:H6; the same statement is recorded in `slice-13-renderer-performance.md`._

S20:H6 sets the standing order explicitly: performance first, code maintainability second, distribution size third, each at least as important as prompt fidelity, and says it “cannot drop.” The existing `godot-performance-evidence` skill only requires measurement before optimisation; the gate stages documented in `docs/GATE.md` contain no runtime-performance or distribution-size check, and no session prompt or proposal field records this ordering. This is therefore a high-priority standing preference, but only one independent occurrence; it should not be presented as a repeated correction yet.

**Proposed kit/process change** — add an immutable priority block to every game-builder session prompt and require each proposal to record its performance, maintainability, distribution-size, and prompt-fidelity impacts/trade-offs. Add report-only budget fields for runtime measurements and exported-size measurements so a slice cannot claim success without saying which priorities were measured. Keep the human approval decision intact; this is a prompt/schema/process contract, not a game-code rule.

**Confidence** — medium for recurrence, high for the preference itself: the human stated it unambiguously and explicitly asked that retrospectives preserve it, but there is one independent occurrence.
**Cost** — medium: prompt/template, proposal schema, renderer, and validation/test fixtures; higher if size budgets become blocking.
**Mechanically checkable** — partly. Presence, ordering, required fields, and numeric evidence are checkable; whether an agent genuinely made the trade-off correctly still requires human review.
